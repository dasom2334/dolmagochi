// 상점 소품 · 대사에 등장하는 사물 — 아트 좌표(96×72) 기준 수작화.
//
// 이 크기(대개 10px 안팎)에서는 절차 생성이 오히려 부정확하다. 문자 격자로 찍고
// 문자→팔레트 슬롯 맵을 따로 둔다. 슬롯이라 시간·계절·날씨 오버레이를 그대로 받는다.
//
//   . = 빈칸. 그 외 문자는 각 소품의 map 이 정의한다.
//
// 배치 제약 (기획서):
//   - 창 아래(x27~66, y35~48)에는 가구를 두지 않는다
//   - 방당 대형 가구 2개까지 — 어느 걸 동시에 놓을지는 게임이 정한다
//
// 좌표는 [x, y] = 격자 왼쪽 위 모서리.

/** 공통 슬롯 — 재질별로 묶어 두면 소품이 늘어도 팔레트가 안 불어난다 */
export const PROP_SLOTS = {
  // 돌 방석 — 분홍 계열. 나무 창턱 위에서 유일한 채도 있는 면이라 눈이 먼저 간다
  '--cu0': '#8f3d5c', '--cu1': '#c8688a', '--cu2': '#eca0b4',   // 방석(분홍)
  // 가구 나무 — 채도를 올리면 보라 계열 방에서 혼자 튄다. 레퍼런스처럼 **눌린 갈색**으로.
  '--wd0': '#33262a', '--wd1': '#4d3b36', '--wd2': '#6a5347',   // 나무(책장·창턱선반)
  // 벽난로 돌 — 나무와 같은 계열이되 조금 더 회색으로(레퍼런스도 둘이 가깝다)
  '--st0': '#2b2228', '--st1': '#463a3d', '--st2': '#61504c', '--st3': '#7a6357',
  '--mt0': '#3f444c', '--mt1': '#666d78', '--mt2': '#949ca8',   // 금속
  '--cer0': '#7d838b', '--cer1': '#b9c0c8', '--cer2': '#e2e7ec', // 도자기
  '--lqt': '#7a4622', '--lqw': '#4a6f8e',                        // 액체(차 / 물)
  // 천(담요) — 붉은 계열. 단, 러그(#87~#aa 대의 붉은색)보다 **밝은 쪽**이어야 한다.
  // 처음엔 러그보다 어둡게 잡았다가 러그에 그대로 먹혔다. 레퍼런스도 러그는 진한 빨강,
  // 담요는 그보다 밝은 분홍빛 — 같은 색상에서 갈리는 건 명도지 색상이 아니다.
  // 담요는 화면 **맨 아래 가운데** — 비네트·AO가 제일 세게 누르는 자리다.
  // 러그와 나란히 놓고 고른 값은 거기서 갈색으로 주저앉는다 → 눌릴 걸 감안해 올려 잡는다.
  // 담요 — 6단 램프. 4단으로는 천의 굴곡을 못 낸다. 깃(칼라)이 제일 밝고
  // 바깥으로 갈수록 어두워지며, 바닥에 닿는 면이 가장 어둡다.
  // 창빛 웅덩이(screen)가 떨어지는 자리라 G·B 를 낮게 깔아야 붉게 남는다.
  // 담요가 러그 위에 놓이는데 **값이 거의 같아** 통째로 묻혔다.
  // 러그(#57132a~#8f1f31)보다 몸통 톤이 확실히 위여야 천이 분리돼 보인다.
  // 아래 두 단은 접힘 그늘용으로만 남기고, 면적을 차지하는 2~4단을 끌어올렸다.
  // 담요 — **레퍼런스에서 뽑은 값 그대로**(design/reference/livingroom-ref3.png).
  // 담요가 초록이라 빨간 러그와 색상으로 깨끗이 갈린다 → 손으로 색을 고를 필요가 없었다.
  // 앞선 붉은 담요 판이 전부 실패한 건 레퍼런스의 담요와 러그가 같은 빨강이라
  // 분리가 안 됐고, 그래서 내가 지어냈기 때문이다.
  // 톤1~5 = 추출한 다섯 밝기 구간의 평균색.
  '--bl0': '#2e3319', '--bl1': '#3b4122', '--bl2': '#484f27',
  '--bl3': '#545c2b', '--bl4': '#656b30', '--bl5': '#787e36',
  // 화분 — 실측값(--pl0 #495633 / --pl1 #757e25, 테라코타 #c1611c #e08a3e)이
  // 이 씬에서 혼자 형광이라 **빛나는 것처럼** 보였다. 낮 오버레이에 saturation 을
  // 넣으면서 더 튀었다. 채도를 한 단씩 죽여 벽·바닥의 탁한 보라와 붙인다.
  // --pl0/--pl1 은 room-data 팔레트에 있지만 resolve() 가 BASE(=PROP_SLOTS)를
  // 나중에 덮으므로 여기 값이 이긴다.
  '--pl0': '#3f4a34', '--pl1': '#66703f', '--pl3': '#8b9a5e',    // 잎 그늘/중간/밝음
  '--pot0': '#6e4230', '--pot1': '#8f5c3c', '--pot2': '#ab7a55', // 테라코타 화분
  // 새 — **무테**. 검은 테두리를 두르면 이 씬에서 혼자 만화가 된다(다른 소품은
  // 전부 색면 3~4단으로만 형태를 낸다). 테두리가 아니라 명도로 딴다.
  //
  // 세 번 고쳐서야 잡았다. 스프라이트만 따로 뽑아 보니 원인이 한눈에 보였다 —
  // **#3a1f12 한 값이 rect 14개**로 왼쪽 3분의 2를 통째로 덮고 있었다.
  // 머리·등·꼬리가 전부 같은 색이니 내부 형태가 아예 없었다. 톤 폭을 벌려도
  // 그 폭을 **쓰는 자리가 없으면** 평면이다.
  // → 몸통을 다섯 구역으로 쪼갠다. 빛은 창(오른쪽 위)에서 오므로 위·오른쪽이 밝다:
  //     bd0 꼬리·다리 → bd1 등 그늘·날개 그늘 → bd2 날개 깃 → bd3 등 → bd4 머리·가슴
  //   날개를 등보다 **어둡게** 둬야 경계가 생긴다(같으면 날개가 등에 먹힌다).
  //   bd7(정수리·뺨)은 머리 윗면이 빛을 정면으로 받는 자리라 한 단 더 밝다.
  //   날개 아래 가장자리엔 W/w 를 한 칸씩 엇갈려 **깃 끝**을 낸다.
  //   bd6(배)은 가장 밝은데 **가장자리가 아니라 안쪽**(idx 8~9)에 둔다 —
  //   가장자리에 두면 그게 역광이 되고, 그게 앞판에서 "역광이 세다"던 문제였다.
  '--bd0': '#2a1610', '--bd1': '#452718', '--bd2': '#6b3c1f',   // 꼬리·다리/그늘/날개깃
  '--bd3': '#8a4f28', '--bd4': '#b06a32', '--bd6': '#d8a165',   // 등/머리·가슴/배
  '--bd7': '#c9843f',                                           // 정수리·뺨 하이라이트
  // 돌 상태 오버레이 — 이끼·젖음·눈·새싹. 돌 본체(--o*)는 그대로 두고 위에 덮는다.
  // 알파를 안 쓰고 **불투명 색면**으로 낸 건 이 씬의 다른 소품과 같은 방식이라
  // 광원 레이어와 싸우지 않기 때문이다(반투명은 screen 웅덩이에서 흰 얼룩이 된다).
  '--om': '#4a6b32',                                            // 이끼
  '--ow': '#3a4258', '--oh': '#c8d4e4',                         // 젖은 아랫면 / 물기 반사
  '--os': '#eef4fb',                                            // 돌에 쌓인 눈
  '--sp0': '#4a7a3a', '--sp1': '#6fa851',                       // 새싹 잎 그늘/밝음
  '--sp2': '#7a6a3a', '--sp3': '#6b5a2e',                       // 줄기 / 시든 잎
  '--bd5': '#140b0f',                                           // 눈
  '--sd0': '#25563d', '--sd1': '#3f8a5c', '--sd2': '#6fb98a',   // 탄산캔
  '--gl0': '#8ba4b6', '--gl1': '#cfe0ea', '--gl2': '#f0f8fc',   // 유리 — 테/속/테두리광
  // 머그 — 레퍼런스(ref3) 추출값. 보라 도자기였는데 레퍼런스는 따뜻한 황토빛이라
  // 러그·책과 한 계열로 묶인다. --mgi 는 트인 입구로 보이는 안쪽 어둠.
  '--mg0': '#613e2c', '--mg1': '#885838', '--mg2': '#a46a3e',   // 머그(황토 도자기)
  '--mgi': '#3a2418',                                           // 머그 안쪽
  // 펼친 책 지면 — 레퍼런스(ref3) 추출값. 예전엔 도자기 슬롯(--cer*)을 같이 써서
  // 책이 흰 사기그릇처럼 보였다. 종이는 따뜻한 황갈이어야 한다.
  // 책 지면. 처음엔 추출 칸의 **5분위 평균**을 그대로 썼는데, 평균이 톤 폭을 압축해
  // 세 단이 서로 4% 안쪽(#bc824f/#d59c60/#e6aa68)이 됐다. 화면에 찍힌 값으로 재 보니
  // 이웃 단 차이가 9/255 — 사람 눈엔 한 덩어리다. 접힘·표지를 아무리 잘 넣어도
  // 지면이 뭉개지면 구조가 안 보인다. → 같은 색상 축에서 명도만 넓게 벌린다.
  '--bp0': '#9c6438', '--bp1': '#c98d52', '--bp2': '#f0cf95',   // 책 지면 그늘/중간/밝음
  '--lqw2': '#6f9dbd',                                           // 물 — 수면(빛 받는 면)
};

/** [x, y, 격자, 문자→슬롯] */
export const ROOM_PROPS = {
  // 돌 방석 — 창턱 돌(x53~62, 밑변 y35)이 **파묻힌** 것처럼 보여야 한다.
  // 평평한 깔개를 돌 밑에 깔면 그냥 돌 밑의 판이다. 푹신함은 두 가지로 만든다:
  //   ① 위로 볼록한 실루엣(가운데가 높고 가장자리로 흘러내림)
  //   ② 앞 테두리가 **돌 밑동을 덮는다**(p-cushion-front, 돌보다 나중에 그린다)
  'p-cushion': [49, 31, [
    '....oooooooo....',
    '..oommmmmmmmoo..',
    '.ommmmmmmmmmmmo.',
    'ommmmmmmmmmmmmmo',
    'ommmmmmmmmmmmmmo',
  ], { o: '--cu0', m: '--cu1' }],

  // 돌 양옆으로 부풀어 오른 부분 + 앞 테두리. 돌보다 **나중에** 그려 밑동을 덮는다
  'p-cushion-front': [49, 34, [
    'ommh..........mo',
    'oommhhhhhhhhmmoo',
  ], { o: '--cu0', m: '--cu1', h: '--cu2' }],




  // 담요 — 두 가지 모습. 책을 읽는 동안은 돌을 감싸고, 아니면 개어 둔다.
  //
  // 개어 둔 모습: **책장 아래 칸**. 바닥에 두면 러그 위에 천이 널브러진 것으로
  // 읽혀서 '치워 둔 상태'가 안 됐다. 접힌 층(밝은 접힘선 ↔ 어두운 골)을
  // 번갈아 쌓아야 개어 둔 것으로 보인다 — 단색 덩어리는 그냥 방석이다.
  // 좌표는 **끌어내기 전** 책장 좌표계 — generate.js 가 책장과 같은 배율로 옮긴다.
  // 6폭이던 것을 **책장 칸 폭**(안쪽 x101~113)에 맞춰 13폭으로 넓혔다.
  // 좁으면 칸 안에 던져 둔 뭉치로 보이지 개어 둔 것으로 안 보인다.
  // 가로 띠 다섯 줄이 접힌 겹이고, 양 끝을 한 단씩 낮춰 모서리를 만든다.
  'p-blanket': [85, 41, [
    '..455555554..',
    '3555555555553',
    '2333333333332',
    '3444444444443',
    '.12222222221.',
  ], { '1': '--bl1', '2': '--bl2', '3': '--bl3', '4': '--bl4', '5': '--bl5' }],

  // 감싼 모습: 러그 돌의 **밑동을 두르는 목도리 꼴**. 레퍼런스에서 **칸 단위로 추출**했다
  // (design/reference/livingroom-ref3.png → tools/ref3_grid.npy, 113×113 격자).
  //
  // 앞선 여러 판이 전부 실패한 건 그때 레퍼런스의 담요와 러그가 **같은 빨강**이라
  // (#da3c24 vs #d83928) 색 임계값도 윤곽선 플러드필도 전부 러그로 샜고,
  // 결국 내가 모양을 지어냈기 때문이다. 이번 레퍼런스는 담요가 초록이라
  // 색상만으로 갈린다 → 아래 격자는 손으로 그린 게 아니라 추출 결과 그대로다.
  //   마스크:  55°≤hue≤170° · S>0.15 · V>0.12   (111칸)
  //   톤:      추출 칸의 밝기 5분위 → 1~5, 색은 각 구간 평균(--bl1~5)
  //
  // 좌표는 **원근 변환 전** 기준이다(렌더러가 바닥 소품을 앞으로 당긴다).
  // 러그 돌은 변환 전 x40~53 / 윗변 y52 / 밑변 y61 — generateGroups 가 돌려주는
  // x56~69 는 변환 **후** 값이라 여기 그대로 쓰면 담요가 돌 오른쪽으로 밀린다.
  // 레퍼런스 돌 x48~62 y67~75(15×9), 내 돌 x40~53 y52~61(14×10) — **거의 1:1**이라
  // 늘리거나 리샘플링하지 않고 평행이동(dx -8, dy -14)만 했다.
  // 가운데가 뚫린 건 그 자리로 돌이 보여야 하기 때문 — 두르는 것이지 덮는 게 아니다.
  // 다만 **돌 오른쪽(col 17~19)** 은 추출 마스크가 비워 놓은 자리라 배경이 비쳤다.
  // 레퍼런스에선 그 자리가 돌 뒤로 돌아 들어가는 천의 안쪽면이다 → 짙은 녹색 명암(1·2)으로
  // 채웠다. 돌에 붙는 쪽이 가장 어둡고 바깥 자락으로 갈수록 밝아진다.
  // 24×11, 아트 x37~60 / y54~64 (변환 전).
  // y 는 레퍼런스대로면 57~67 인데 두 번 옮겼다:
  //   ① 러그 밑변(y66)을 넘어서 57→56
  //   ② **가운데 뚫린 자리가 돌 밑변(y61)보다 아래로 내려가** 돌이 못 채우는 두 줄에
  //      배경이 비쳤다 — 담요가 돌에 안 붙어 보이던 진짜 이유. 56→54 로 두 줄 더 올려
  //      구멍 있는 줄이 전부 돌 안에 들어오게 했다.
  // y 는 레퍼런스 그대로면 57~67 인데 러그 밑변이 y66 이라 마지막 줄이 러그 밖으로
  // 떨어졌다 → 한 줄 올렸다. 목도리가 돌에 1px 높이 걸리지만 러그는 안 벗어난다.
  'p-blanket-wrap': [37, 54, [
    '....5...................',
    '...54...................',
    '...42............1125...',
    '...52............11254..',
    '...54............12554..',
    '..2354...........155524.',
    '4331243........45553244.',
    '32342132......4543235534',
    '121233211124443212454222',
    '..111111343321334431221.',
    '.........111123311......',
  ], { '1': '--bl1', '2': '--bl2', '3': '--bl3',
       '4': '--bl4', '5': '--bl5' }],

  // 화분 — 실측 원본은 4×5 짜리 색점 뭉치라 화분도 잎도 아닌 얼룩이었다
  // (흙그릇 픽셀이 3개뿐). 잎 슬롯(--pl*)만 물려받아 새로 그린다.
  //
  // **무테**. 앞판은 --pl0 을 잎 둘레에 한 바퀴 둘러 윤곽선으로 썼는데, 이 씬의
  // 다른 소품은 전부 색면 3단으로만 형태를 낸다(새도 같은 이유로 무테로 고쳤다).
  // 어두운 테를 두르면 혼자 만화가 되고, 그 테가 명암 노릇까지 해서 광원을
  // 켜고 꺼도 안 변한다. 테를 빼고 **위가 밝고 아래가 어두운** 방향 명암으로 딴다.
  // 창턱 왼쪽 끝, 밑변 y35 = 창턱 선반 윗면.
  'sill-plant': [28, 27, [
    '...h....',
    '..hhh.h.',
    '.hhmhhhh',
    'hmmmmmhm',
    '.mmmmmm.',
    '..dmmd..',
    '.RRRRRR.',
    '.rPPPPr.',
    '..rPPr..',
  ], { d: '--pl0', m: '--pl1', h: '--pl3',
       R: '--pot2', P: '--pot1', r: '--pot0' }],







  // 찻잔 — 창턱 **돌 오른쪽**, 창턱 선반 위(밑변 y35). 스탠드(아트 x70~)와
  // 안 닿게 x64~70 에 앉힌다. 폭 9 → 7 로 좁혔다 — 9는 찻잔이라기엔 대접이었다.
  // 빈 잔은 **안쪽 벽(밝음)과 바닥(그늘)**이 보여야 비었다고 읽힌다 —
  // 안을 통째로 어둡게 칠하면 그냥 검은 구멍이다.
  'p-cup': [64, 31, [
    '.tttt..',
    'cIIIIch',
    'cbeebcH',
    '.cbbc..',
    'ssssss.',
  ], { t: '--cer2', I: '--cer1', e: '--st0', c: '--cer1', b: '--cer0',
       h: '--cer1', H: '--cer0', s: '--cer0' }],

  // 내용물 — 채운 상태에만. 잔 안쪽을 덮는다
  'p-cup-tea': [65, 32, [
    'LLLL',
    '.LL.',
  ], { L: '--lqt' }],

  // 김 — 채운 상태에만. 오르며 옅어진다
  'p-cup-steam': [65, 27, [
    '.w.',
    'w.w',
    '.w.',
    'w.w',
  ], { w: '--cer2' }],

  // 물컵 (대사: restAct.water / act.nurse '이불 옆, 물 한 컵').
  // 담요가 책장 칸으로 올라갔으므로 물컵은 러그 오른쪽 바닥에 그대로 둔다 —
  // 책장 선반 위의 물컵은 '머리맡의 물 한 컵'으로 안 읽힌다.
  // 토글은 담요와 독립이라 물컵만·담요만·둘 다 전부 만들 수 있다.
  // 레퍼런스: **보라 도자기** 머그, 손잡이 오른쪽, 안에 짙은 내용물.
  // 손잡이는 몸통에서 한 칸 띄운다 — 붙이면 손잡이가 아니라 귀다.
  // 흰색이면 책(밝은 지면)과 붙어 둘이 한 덩어리로 보인다. 보라가 그걸 갈라 준다.
  // 머그 — 레퍼런스(ref3) 추출. **손잡이가 왼쪽**, 위가 트여 안쪽 어둠이 보이고
  // 그 밑에 밝은 테 한 줄이 돈다. 앞판은 손잡이가 오른쪽에 붙은 밋밋한 원통이라
  // 컵이 아니라 물병으로 읽혔다.
  // 레퍼런스 x36~42 y78~83(7×6). 레퍼런스대로면 돌밑변+3~8 인데 그 자리엔 책이 와야 해서
  // (내 방이 얕아 책을 위로 당겼다) 머그와 책을 러그 **왼쪽 절반에 위아래로** 쌓았다. 머그가 위(멀리), 책이 아래(가까이).
  // 캔버스 x45~51 y52~57.
  'p-waterglass': [29, 52, [
    '..RRRRR',
    '.hDDDDD',
    'hhRRRRR',
    'h.LBBBB',
    'hhBBBBB',
    '..bbbbb',
  ], { R: '--mg2', D: '--mgi', L: '--mg2', B: '--mg1',
       b: '--mg0', h: '--mg1' }],

  // 풍경(風磬) — 창 오른쪽 벽. 창을 열면 **대롱만** 흔들려야 하므로 따로 뗀다
  'p-windchime': [69, 3, [
    '..W..',
    '.WWW.',
    'WWWWW',
  ], { W: '--mt0' }],

  'p-windchime-tubes': [69, 6, [
    '..t..',
    '..t..',
    '.m.m.',
    '.m.m.',
    '.m.m.',
    '.m.m.',
    '..p..',
    '..p..',
  ], { t: '--wd0', m: '--mt2', p: '--wd1' }],

  // 창밖 턱에 앉은 새 (대사: fore.bird). **유리 바깥**이라 창틀이 위에 덮인다
  // (render.js Z 에서 g-wall 앞) — 방 안에 두면 집 안에 새가 들어와 있는 그림이다.
  //
  // 다섯 판째다. 실패 계보와 거기서 배운 것:
  //   ① 5×4 원본 — 목·부리·눈·날개·꼬리가 하나도 없는 색점 뭉치
  //   ② 갈색 + 검은 테두리 — 테두리로 형태를 따니 혼자 만화가 됐다(톤 안 맞음)
  //   ③ 무테 흰 비둘기 — 폭이 2→4→6→8 로 고르게 벌어져 피라미드
  //   ④ 목을 넣어 세운 자세 — 형태는 잡혔지만 밝은 톤을 **테두리처럼 한 바퀴**
  //      둘러서 그게 곧 림라이트였다(노을 앞에서 통째로 빛남)
  //   ⑤ 테두리를 지웠더니 형태까지 사라짐 — 그 테두리가 명암 노릇도 하고 있었다
  //
  // → 레퍼런스를 다시 골라 **통통한 참새(웅크린 자세)** 로.
  //   11px 스케일에서 세운 자세는 목이 1~2px 라 무슨 수를 써도 뭉개진다.
  //   웅크리면 머리와 몸통이 한 덩어리가 되어 실루엣이 단순해지고, 그만큼
  //   작아도 새로 읽힌다. 목을 없애는 게 후퇴가 아니라 이 크기의 정답이었다.
  //
  // 명암은 **방향을 준다** — 사방에 두르면 그게 림라이트다. 빛은 한 방향에서만 온다.
  // 좌우 반전해 **오른쪽을 보게** 하고 창 왼쪽 칸(찻잔이 있던 자리의 창밖)으로
  // 옮겼다. 반전 덕에 밝은 면이 오른쪽으로 가는데, 해가 창 오른쪽 위에 있으므로
  // 오히려 이쪽이 맞다 — 명암을 따로 뒤집을 필요가 없었다.
  // 11×9, 발끝 y33 = 창 아래턱.
  // ── 돌 상태 오버레이 ─────────────────────────────────────────────
  // 러그 돌(변환 전 x40~53 / y52~61, 14×10)에 **정확히 겹친다**. 돌이 움직이면
  // 여기 좌표도 같이 움직여야 한다 — 실루엣 밖으로 한 칸만 나가도 허공에 뜬다.
  //
  // 기존 레이어 렌더러(RockSprite)가 하던 표현이라 캔버스로 갈아 끼우면서
  // 통째로 사라졌던 것들이다. 돌은 이 셋으로 "지금 무슨 일을 겪었는지" 를 말한다.

  // 이끼 — 상점 '이끼' 배치 시. 볕이 덜 드는 왼쪽과 오른쪽 아래에 앉는다
  'orb-moss': [40, 52, [
    '..............',
    '..............',
    '.m............',
    '.mm...........',
    'mmm.........m.',
    '.mm........mm.',
    '..m........mm.',
    '...........m..',
    '..............',
    '..............',
  ], { m: '--om' }],

  // 비 맞고 들어온 돌 — 아랫면이 젖어 어둡고 윗면에 물기가 반짝인다
  'orb-wet': [40, 52, [
    '..............',
    '...h..........',
    '..h...........',
    '..............',
    '..............',
    '..............',
    '.w..........w.',
    'ww..........ww',
    '.www......www.',
    '..wwwwwwwwww..',
  ], { w: '--ow', h: '--oh' }],

  // 눈 맞고 들어온 돌 — 위 실루엣을 따라 쌓인다
  'orb-snow': [40, 52, [
    '....ssssss....',
    '..ss.s....ss..',
    '.s..........s.',
    '..............',
    '..............',
    '..............',
    '..............',
    '..............',
    '..............',
    '..............',
  ], { s: '--os' }],

  // 돌 위 새싹 — 돌 꼭대기(x44~49)에 얹혀 위로 자란다. 밑동 y51 = 돌 윗변 바로 위.
  // 게임의 SproutStage 는 budding/thriving/rooting1/rooting2/시듦(숫자) 인데
  // 여기 그림은 **셋**뿐이다. 단계별 그림이 더 필요해지면 여기 늘린다.
  'orb-sprout-bud': [44, 46, [
    '......',
    '......',
    '......',
    '..gG..',
    '..s...',
    '..s...',
  ], { g: '--sp0', G: '--sp1', s: '--sp2' }],

  'orb-sprout-thrive': [44, 46, [
    '..GG..',
    '.GgGG.',
    'gGGGGg',
    '.GgGg.',
    '..ss..',
    '..s...',
  ], { g: '--sp0', G: '--sp1', s: '--sp2' }],

  'orb-sprout-wither': [44, 46, [
    '......',
    '.d...d',
    '.dd.dd',
    '..d.d.',
    '..ss..',
    '..s...',
  ], { d: '--sp3', s: '--sp2' }],

  'p-bird': [34, 25, [
    '.....HHh...',
    '....HHhhhk.',
    '...dHhhehkk',
    '..ddWWWhbb.',
    'ttDdWWWwBBb',
    '.tDdWWwwBBb',
    '..DDWwWwbB.',
    '...DDwwbb..',
    '.....ff.f..',
  ], { t: '--bd0', f: '--bd0', D: '--bd1', w: '--bd1', W: '--bd2',
       d: '--bd3', h: '--bd4', k: '--bd4', b: '--bd4', H: '--bd7',
       B: '--bd6', e: '--bd5' }],
};

/** 문자 격자 → rect. 가로 런 병합까지 여기서 한다 */
export function buildRoomProps() {
  const out = {};
  for (const [id, [ox, oy, art, map]] of Object.entries(ROOM_PROPS)) {
    const rects = [];
    art.forEach((row, j) => {
      let i = 0;
      while (i < row.length) {
        const ch = row[i];
        if (ch === '.') { i++; continue; }
        let k = i;
        while (k + 1 < row.length && row[k + 1] === ch) k++;
        const slot = map[ch];
        if (slot) rects.push([ox + i, oy + j, k - i + 1, 1, slot]);
        i = k + 1;
      }
    });
    out[id] = rects;
  }
  return out;
}
